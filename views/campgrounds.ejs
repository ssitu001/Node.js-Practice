<% include partials/header %>

<h1>This is the campgrounds page</h1>
  <a href="/campgrounds/new">Add a new campground!</a>
  <% campgrounds.forEach(function(camp){ %>
  <div>
    <h4><%= camp.name %></h4>
    <img src="<%= camp.image %>" width=200px height=200px>
  </div>
<% })%>

<% include partials/header %>